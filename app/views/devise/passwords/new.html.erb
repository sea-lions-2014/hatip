<div class="devise-form">
<div class="form-container">

<h1 class="devise-title">Forgot password?</h1>

<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="form-field"><%= f.email_field :email, :autofocus => true, placeholder: "E-Mail Address" %></div>

  <div><%= f.submit "Reset Password", class: "success-button" %></div>
<% end %>

</div>
</div>
